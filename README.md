Action.sh deletes all containers and nukes the machines. 
if you want it to download the runner for rancher again apply the docker run file in clean.sh

Konflikt.sh handles the container conflict error. 

